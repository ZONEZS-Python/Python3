# 列表统计及删除方法拓展

### <a name="qaqfxg"></a>len()方法
### <a name="tgs5ik"></a>列表的count语法
```python
name_list = ["张三", "李四", "王五"]

# len(length) 函数可以统计列表中元素的总数
list_len = len(name_list)
print(list_len)

# count 方法可以统计某一个元素在列表中出现的次数
count = name_list.count("张三")
print("张三出现了 %d 次" % count)
```

